---
- name: Run unit tests
  hosts: tester
  vars:
    tester:
      component:
          tox_target: unittest
  roles:
      - { role: setup_repos }
      - { role: setup_environment }
      - { role: install_packages }
      - { role: collect_logs }
      - { role: run_tests }
