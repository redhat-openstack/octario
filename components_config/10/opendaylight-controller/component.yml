include_vars:
    file: "{{ playbook_dir }}/../components_config/common/opendaylight.yml"

test_config:
  virt:
    RedHat-7:
      setup:
        install: "{{ rpm_deps }}"
        remove: "{{ remove_rpm }}"
        enable_repos: [ ]
      run: "{{ opendaylight_virt_run_config.run }}"
      archive: "{{ archive }}"