# Tempest
# -------

# this is probably for newer versions
#tempest_regex: sahara_tempest_plugin\.tests

# this should be fine for rhos7 and below
#tempest_regex: tempest.*\.data_processing

# keep the single quotes inside the string for the sake of bash command using it
tempest_regex: "'(tempest.*\\.data_processing|sahara_tempest_plugin\\.tests)'"


# Dist-git
# --------
component_dist_git_name: 'openstack-sahara'
component_dist_git_url: 'git://pkgs.devel.redhat.com/rpms/openstack-sahara'
